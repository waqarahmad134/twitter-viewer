export const metadata = {
  title: "Admin Login | Twitter Viewer",
  description: "Login to manage blog posts and SEO.",
};

export default function AdminLoginLayout({ children }) {
  return children;
}

