# Copy to nitter.conf and edit
# Full options: https://github.com/zedeus/nitter/blob/master/nitter.example.conf

hostname = "nitter"
port = 8080
https = false

[Redis]
host = "redis"
port = 6379

[sessions]
# Loads from sessions.jsonl in same directory
# Or set NITTER_SESSIONS_FILE env
